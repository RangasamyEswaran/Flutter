import 'package:flutter/material.dart';

class TransferOwn extends StatelessWidget {
  const TransferOwn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
    );
  }
}